ecbuild_add_executable( TARGET      eckit_version
                    	SOURCES     eckit_version.cc
                        LIBS        eckit )

ecbuild_add_executable( TARGET      eckit_read_config
                    	SOURCES     eckit_read_config.cc
                        LIBS        eckit )
