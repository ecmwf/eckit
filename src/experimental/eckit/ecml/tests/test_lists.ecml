test, label = lists produced by expressions embedded in a list are concatenated together,
do = (
        let, x = 1 / 2 
        println,values = (value,of = x) / (value,of = x)
),
expect = 1 / 2 / 1 / 2
