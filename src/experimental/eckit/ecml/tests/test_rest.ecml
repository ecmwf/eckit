test, label = rest returns a copy of the list supplied in values parameter without its first element,
do = (
    println, values = (rest, of = foo / bar / baz)
),
expect = bar / baz

test, label = check return value of rest is not embedded in an extra list,
do = (
    let, x = 1 / 2 / 3 / 4 / 5
    println,values = (first,of = (rest, of = (value, of = x)))
),
expect = 2
